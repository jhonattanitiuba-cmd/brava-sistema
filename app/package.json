{
  "name": "brava-app",
  "version": "1.0.0",
  "private": true,
  "description": "Brava Company - App (Login + Onboarding + CRM)",
  "scripts": {
    "build": "echo 'Static site - no build needed' && exit 0"
  }
}
