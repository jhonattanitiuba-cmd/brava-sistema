{
  "name": "brava-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Brava Company - CRM / Login / Onboarding",
  "scripts": {
    "build": "echo 'Static site - no build needed' && exit 0"
  }
}
